$(function () {
    window.onload = function() {
        if (window.jQuery) {
            // jQuery is loaded
            alert("jQuerry Works!");
        } else {
            // jQuery is not loaded
            alert("Doesn't Work");
        }
    }


})